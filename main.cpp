#include<bits/stdc++.h>
using namespace std;

int main() {
    int input;
    while (cin >> input && input != 0) {
        int output = input;

        cout << output << endl;
    }

    return 0;
}

